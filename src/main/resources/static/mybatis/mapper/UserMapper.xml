<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java4.asm2polyoe.mapper.UserMapper">
    <select id="findAll" resultType="User">
        SELECT Id as id, Password as password, Fullname as fullname, Email as email, Admin as admin 
        FROM Users
    </select>

    <select id="findById" parameterType="string" resultType="User">
        SELECT Id as id, Password as password, Fullname as fullname, Email as email, Admin as admin 
        FROM Users WHERE Id = #{id}
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO Users (Id, Password, Fullname, Email, Admin)
        VALUES (#{id}, #{password}, #{fullname}, #{email}, #{admin})
    </insert>

    <update id="update" parameterType="User">
        UPDATE Users
        SET Password = #{password},
            Fullname = #{fullname},
            Email = #{email},
            Admin = #{admin}
        WHERE Id = #{id}
    </update>

    <delete id="delete" parameterType="string">
        DELETE FROM Users WHERE Id = #{id}
    </delete>
</mapper>